.DS_Store
node_modules/
.kg-data/
# Knowledge graph: track graph.json + GRAPH_REPORT.md (they ride PRs per
# /stone-commit); everything else under graphify-out/ is local cache/render.
graphify-out/*
!graphify-out/graph.json
!graphify-out/GRAPH_REPORT.md
.journal/
*.log
.claude/settings.local.json
.claudeai-build/
*.zip

# Adopt-Pocock workbench (v2-durability engine): local-only, never pushed.
# The live index+siblings grow per-install; only templates+spec under
# skills/in-progress/stone-adopt-pocock/references/ are tracked. See #65.
workbench/
