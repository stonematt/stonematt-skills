# skills/personal

Tied to specific personal setups, not promoted in `plugin.json` or the top-level README.

_(empty — identity-bearing content lives in the private dotfiles `stonematt-skills` stow package, not here)_
